---
import StandaloneLayout from '../layouts/StandaloneLayout.astro';
import WaitlistSection from '../components/WaitlistSection.astro';

const pageTitle = 'Join Hypr MCP Waitlist - Get Early Access to MCP Platform';
const pageDescription =
  'Join Hypr MCP waitlist for early access to serverless MCP hosting. Be first to deploy Model Context Protocol servers with one command. Beta soon.';
---

<StandaloneLayout
  title={pageTitle}
  description={pageDescription}
  ogUrl="https://hyprmcp.com/waitlist/"
  footerTagline="Join the Hypr MCP Waitlist."
  footerLinks={[]}>
  <WaitlistSection />
</StandaloneLayout>
